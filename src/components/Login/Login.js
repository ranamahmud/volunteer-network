import React from 'react';
import { Container } from 'react-bootstrap';

const Login = () => {
    return (
        <Container>
            <p>Login With</p>
        </Container>
    );
};

export default Login;